--- 
clone: 
  depth: 1
kind: pipeline
name: WimbiPipeline
steps: 
  - 
    commands: 
      - "curl https://raw.githubusercontent.com/STRIX-Project/kernel_build_script/droneci/droneci-release-X00T.sh | bash"
    environment: 
      TELEGRAM_TOKEN: 
        from_secret: BOT_API_KEY
    image: fadlyas/kernel_dockerfile:latest
    name: compile
    trigger: 
      branch: 
        - STRIX-EAS-OC
